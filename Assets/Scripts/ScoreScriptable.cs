using System.Collections.Generic;
using UnityEngine;

// [System.Serializable]

[CreateAssetMenu(fileName = "ScoreScriptable", menuName = "ScriptableObjects/ScoreScriptable", order = 1)]
public class ScoreScriptable : ScriptableObject
{
    public int score;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
